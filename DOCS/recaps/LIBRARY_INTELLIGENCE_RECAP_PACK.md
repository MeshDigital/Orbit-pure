# Library Intelligence Recap Pack

Status: Active
Last Updated: 2026-06-04

## Chain
- Previous: DOCS/LIBRARY_SIDEBAR_UNIFICATION_CLOSURE_RECAP_PACK_2026-05-29.md
- Current: DOCS/recaps/LIBRARY_INTELLIGENCE_RECAP_PACK.md
- Next: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md

## Slice Ledger
- LI-001: Completed (scanner contract and relative-path normalization regression)
- LI-002: Completed (section-vector null/blank tolerance and neutral fallback defaults)
- LI-003: Completed (matching/similarity scoring constants centralized and reconciled)
- LI-004: Completed (library health cadence hardening with drift-aware refresh checks)
- LI-005: Completed (sparse metadata candidate fallback contracts pinned in tests)
- LI-006: Completed (folder registration dedupe consolidation and transient retry semantics)
- LI-007: Completed (analysis queue duplicate in-flight suppression for coherent status state)
- LI-008: Completed (discoverability/index sync for intelligence contract updates)
- LI-009: Completed (strict auto-download compatibility hardening for per-track format and bitrate overrides)
- LI-010: Completed (lane closure checkpoint with pending risk register)
- LI-011: Completed (lane queue continuation extended through LI-014)
- LI-012: Completed (strict-mode override compatibility regression contract pinned)
- LI-013: Completed (discoverability/index/recap sync for LI-009 and LI-010 outputs)
- LI-014: Completed (continuation checkpoint with next-slice pointer)
- LI-015: Completed (strict filter policy centralized for query/candidate parity)
- LI-016: Completed (duration-unit normalization guard added for strict duration filtering)
- LI-017: Completed (parity/duration normalization regression contracts pinned)
- LI-018: Completed (risk register reconciled after mitigation rollout)
- LI-019: Completed (discoverability/index/recap/memory sync for LI-015..LI-018 outputs)
- LI-020: Completed (continuation checkpoint with next-slice pointer)
- LI-021: Completed (sparse-metadata strict acceptance floor and confidence adjustments)
- LI-022: Completed (sparse-metadata strict acceptance regression contracts)
- LI-023: Completed (strict+Library compatibility gate task added)
- LI-024: Completed (combined compatibility gate baseline executed)
- LI-025: Completed (risk register reconciled for remaining open risks)
- LI-026: Completed (continuation checkpoint with next-slice pointer)
- LI-027: Completed (scorer continuation wave risk intake and hardening target map)
- LI-028: Completed (nullable scorer dereference removed in bitrate/size scoring path)
- LI-029: Completed (sparse-metadata and null-options scorer regression contracts pinned)
- LI-030: Completed (LI compatibility gate expanded to include MatchScorer contracts)
- LI-031: Completed (discoverability/index/recap/risk/memory sync for LI-027..LI-030 outputs)
- LI-032: Completed (continuation checkpoint with next-slice pointer)
- LI-033: Completed (scorer risk intake reopened for fallback-policy and format-normalization drift)
- LI-034: Completed (MatchScorer format normalization and optional MP3 fallback handling hardened)
- LI-035: Completed (scorer regression contracts pinned for fallback uplift and extension normalization)
- LI-036: Completed (scorer-inclusive compatibility baseline refreshed)
- LI-037: Completed (discoverability/index/recap/risk/memory sync for LI-033..LI-036 outputs)
- LI-038: Completed (continuation checkpoint with next-slice pointer)
- LI-039: Completed (strict-policy parity hardened so OnHold MP3 path is config-gated)
- LI-040: Completed (MatchScorer bitrate transcode guard hardened with normalized extension-derived format detection)
- LI-041: Completed (fallback-parity and extension-derived bitrate guard regression contracts pinned)
- LI-042: Completed (scorer-inclusive compatibility baseline refreshed)
- LI-043: Completed (discoverability/index/recap/risk/memory sync for LI-039..LI-042 outputs)
- LI-044: Completed (continuation checkpoint with next-slice pointer)
- LI-051: Completed (strict/scorer malformed-format parity hardened with extension-fallback allowlist retry behavior)
- LI-052: Completed (strict/scorer MIME-style format normalization parity hardened with extension-missing candidate coverage)
- LI-053: Completed (strict-policy normalization risk intake for adapter-boundary MIME-format drift)
- LI-054: Completed (strict policy MIME-style normalization hardening)
- LI-055: Completed (adapter-boundary MIME-format regression contract coverage)
- LI-056: Completed (validation baseline refresh)
- LI-057: Completed (governance sync for LI-053..LI-056 outputs)
- LI-058: Completed (continuation checkpoint with next-slice pointer)
- LI-059: Completed (strict/scorer risk intake for MIME subtype alias canonicalization and metadata-fragment drift)
- LI-060: Completed (strict/scorer/policy MIME alias canonicalization and metadata-fragment token hygiene hardening)
- LI-061: Completed (strict/scorer/adapter-boundary regression contracts pinned for MIME alias canonicalization and token filtering)
- LI-062: Completed (validation baseline refresh)
- LI-063: Completed (governance sync for LI-059..LI-062 outputs)
- LI-064: Completed (continuation checkpoint with next-slice pointer)
- LI-065: Completed (prefetch strict-verification risk intake for format-policy drift)
- LI-066: Completed (prefetch format gate hardening for strict-policy parity)
- LI-067: Completed (prefetch regression contracts pinned)
- LI-068: Completed (validation baseline refresh)
- LI-069: Completed (governance sync for LI-065..LI-068 outputs)
- LI-070: Completed (continuation checkpoint with next-slice pointer)
- LI-071: Completed (strict diagnostic risk intake for candidate format-description drift)
- LI-072: Completed (candidate diagnostics format rendering hardening)
- LI-073: Completed (strict diagnostic regression contracts pinned)
- LI-074: Completed (validation baseline refresh)
- LI-075: Completed (governance sync for LI-071..LI-074 outputs)
- LI-076: Completed (continuation checkpoint with next-slice pointer)
- LI-077: Completed (scorer allowlist risk intake for malformed token drift)
- LI-078: Completed (scorer allowlist malformed-token hygiene and default fallback hardening)
- LI-079: Completed (scorer malformed-allowlist regression contracts pinned)
- LI-080: Completed (validation baseline refresh)
- LI-081: Completed (governance sync for LI-077..LI-080 outputs)
- LI-082: Completed (continuation checkpoint with next-slice pointer)
- LI-083: Completed (adapter-bound normalization risk intake for helper streaming-path parity)
- LI-084: Completed (adapter-bound allowed-format normalization and filter-token hygiene hardening in `SoulseekSearchHelper`)
- LI-085: Completed (adapter-bound helper regression contract pinned)
- LI-086: Completed (validation baseline refresh)
- LI-087: Completed (governance sync for LI-083..LI-086 outputs)
- LI-088: Completed (continuation checkpoint with next-slice pointer)
- LI-089: Completed (scorer trusted-source reliability risk intake)
- LI-090: Completed (trusted-source matching normalization hardening in `MatchScorer`)
- LI-091: Completed (trusted-source normalization regression contract pinned)
- LI-092: Completed (validation baseline refresh)
- LI-093: Completed (governance sync for LI-089..LI-092 outputs)
- LI-094: Completed (continuation checkpoint with next-slice pointer)
- LI-095: Completed (helper malformed-allowlist parity risk intake)
- LI-096: Completed (helper malformed-allowlist hygiene and default fallback hardening)
- LI-097: Completed (helper malformed-allowlist regression contract pinned)
- LI-098: Completed (validation baseline refresh)
- LI-099: Completed (governance sync for LI-095..LI-098 outputs)
- LI-100: Completed (continuation checkpoint with next-slice pointer)
- LI-101: Completed (comma-delimited MIME normalization parity risk intake)
- LI-102: Completed (strict/helper/scorer metadata-separator normalization hardening)
- LI-103: Completed (comma-delimited MIME alias regression contracts pinned)
- LI-104: Completed (validation baseline refresh)
- LI-105: Completed (governance sync for LI-101..LI-104 outputs)
- LI-106: Completed (continuation checkpoint with next-slice pointer)
- LI-107: Completed (whitespace-delimited MIME normalization parity risk intake)
- LI-108: Completed (strict/helper/scorer whitespace metadata-separator normalization hardening)
- LI-109: Completed (whitespace-delimited MIME canonicalization regression contracts pinned)
- LI-110: Completed (validation baseline refresh)
- LI-111: Completed (governance sync for LI-107..LI-110 outputs)
- LI-112: Completed (continuation checkpoint with next-slice pointer)
- LI-113: Completed (quoted MIME normalization parity risk intake)
- LI-114: Completed (strict/helper/scorer quote-wrapper normalization hardening)
- LI-115: Completed (quoted MIME canonicalization regression contracts pinned)
- LI-116: Completed (validation baseline refresh)
- LI-117: Completed (governance sync for LI-113..LI-116 outputs)
- LI-118: Completed (continuation checkpoint with next-slice pointer)
- LI-119: Completed (wrapper-delimited MIME normalization parity hardening + validation refresh)
- LI-120: Completed (governance sync and queue-120 closure checkpoint)

## Scanner Contract Note
- `EnsureDefaultFolderAsync` is now pinned by regression coverage for missing-folder insertion, trailing-separator dedupe, Windows case-insensitive dedupe, and relative-path normalization.

## Section Vector Contract Note
- `SectionVectorService` now short-circuits null/blank hashes safely and returns neutral defaults when section data is unavailable.
- Regression coverage now pins null/whitespace tolerance and no-section neutral semantics in `SectionVectorServiceTests`.

## Matching Scoring Contract Note
- `ScoringConstants.Matching` now serves as the shared scoring source for `TrackSimilarityService` and `TrackMatchScorer`.
- Regression coverage now pins normalization invariants and transition-score constant usage semantics in `MatchingScoringConstantsTests`.

## Library Health Cadence Contract Note
- `TrackRepository` now increments `LibraryHealthEntity.PendingUpdates` as a drift marker instead of writing partial aggregates during frequent track updates.
- `DashboardService` now owns adaptive refresh decisions via `ShouldRecalculateLibraryHealth(...)` and clears drift markers after full recomputation.
- `MissionControlService` now checks library health cadence every 15 seconds and recalculates only when stale-age or pending-update thresholds indicate drift.
- Regression coverage now pins age/threshold refresh semantics in `DashboardServiceLibraryHealthCadenceTests`.

## Sparse Metadata Candidate Contract Note
- `SuggestNextCandidateViewModel` sparse-track projections are now pinned to safe fallback display semantics for missing title/artist/key/BPM/energy values.
- `PlaylistUpgradeCandidateViewModel` sparse replacement projections and score labels are now pinned for null metadata and out-of-range score/prior inputs.
- Regression coverage now pins these behaviors in `PlaylistIntelligenceSparseMetadataTests`.

## Scanner De-duplication and Retry Contract Note
- `EnsureDefaultFolderAsync` now consolidates multiple normalized-folder rows into a single canonical enabled row to prevent persistent duplicate registrations.
- Folder registration now retries transient database lock/busy failures with bounded backoff before surfacing errors.
- Regression coverage now pins duplicate-row consolidation and disabled-row re-enable semantics in `LibraryFolderScannerServiceTests`.

## Queue Coherence Contract Note
- `AnalysisQueueService` now suppresses duplicate in-flight requests for the same `TrackGlobalId` using scheduled-track de-duplication.
- Queue status publishing remains transparent while preventing duplicate queue-count inflation from repeated requests.
- Regression coverage now pins duplicate in-flight suppression behavior in `AnalysisQueueTests`.

## Auto-Download Intelligence Compatibility Contract Note
- `AutoSearchService` candidate filtering now honors per-track `PreferredFormats`, aligning strict candidate gates with intelligence-enriched track-level format policy.
- Non-positive per-track `MinBitrateOverride` values now safely fall back to configured strict-mode bitrate defaults, preventing sparse/intelligence-side override drift from weakening quality gates.
- Regression coverage now pins preferred-format precedence and zero/positive bitrate-override behavior in `AutoSearchServiceTests`.

## Strict Filter Policy Parity Contract Note
- `AutoDownloadStrictFilterPolicy` now centralizes allowed-format and bitrate-resolution logic shared by `AutoSearchService` and `SoulseekSearchHelper`, preventing query-time and candidate-time policy drift.
- Strict duration gating now normalizes `PlaylistTrack.CanonicalDuration` values when upstream metadata arrives in seconds-scale units, preserving expected duration filtering behavior.
- Regression coverage now pins preferred-format fallback parity and seconds-scale duration normalization behavior in `AutoSearchServiceTests` and zero-override bitrate-token parity in `SoulseekSearchHelperTests`.

## Sparse Metadata Acceptance Contract Note
- `AutoSearchService` now applies metadata-confidence adjustments for sparse tracks and enforces a strict acceptance floor even when global minimum score configuration is lowered.
- Regression coverage now pins sparse-floor rejection and non-sparse acceptance behavior in `AutoSearchServiceTests`.

## Cross-Lane Compatibility Gate Contract Note
- `.vscode/tasks.json` now includes `Phase2: Test Gate (LI Compatibility)` and `Phase2: Validation (LI Compatibility)` to execute strict auto-download + Library coverage in a single lane-compatible run.
- Compatibility gate baseline now validates strict-focused and Library surfaces together to prevent hidden cross-lane regressions.

## MatchScorer Hardening Contract Note
- `MatchScorer` now null-safely resolves `MinFileSizeBytes` when options are absent, eliminating nullable dereference risk in bitrate/size scoring.
- Regression coverage now pins sparse-metadata conservative scoring and null-options bounded-score safety in `MatchScorerTests`.

## MatchScorer Fallback Normalization Contract Note
- `MatchScorer` now normalizes candidate and allowed extensions (trim, leading-dot removal, lowercase) before format allowlist checks, preventing false rejects from dotted/whitespace format inputs.
- When `AllowMp3Fallback` is enabled, MP3 candidates can receive a bounded fallback format score instead of unconditional rejection, preserving conservative lossless preference while honoring fallback policy.
- Regression coverage now pins MP3 fallback uplift and dotted/whitespace extension normalization behavior in `MatchScorerTests`.

## OnHold Fallback Policy Parity Contract Note
- `AutoDownloadStrictFilterPolicy` now applies OnHold MP3-only mode only when `EnableMp3Fallback` is enabled, aligning strict query/candidate policy with discovery fallback-profile gates.
- Regression coverage now pins config-disabled OnHold behavior to prevent unconditional MP3 forcing in strict filtering paths.

## Extension-Derived Bitrate Guard Contract Note
- `MatchScorer` bitrate transcode detection now normalizes format values before FLAC low-bitrate checks and falls back to normalized filename extension when `Track.Format` is empty.
- Regression coverage now pins extension-derived FLAC transcode detection to prevent dotted/whitespace format drift in bitrate guards.

## Expanded LI Compatibility Gate Contract Note
- `.vscode/tasks.json` compatibility gate now includes `FullyQualifiedName~MatchScorerTests` so scorer contracts run in the same cross-lane validation path as strict auto-download and Library surfaces.
- Compatibility validation baseline now includes scorer contracts to reduce silent drift between strict candidate acceptance and scorer behavior.

## Malformed Format Metadata Parity Contract Note
- `SoulseekSearchHelper.FilterCandidates` and `MatchScorer.ScoreFormat` now retry allowlist checks with normalized filename extension fallback when candidate format metadata is non-empty but unrecognized.
- Regression coverage now pins malformed-format fallback behavior in `SoulseekSearchHelperTests` and `MatchScorerTests`.
- Validation baseline refreshed: strict+scorer focused gate PASS (53/53) and strict+Library+scorer compatibility gate PASS (134/134).

## MIME-Style Format Normalization Parity Contract Note
- `SoulseekSearchHelper.FilterCandidates` and `MatchScorer` format gates now normalize MIME-style candidate format metadata (for example `audio/x-flac; charset=utf-8`) to allowlist-compatible extensions before fallback behavior.
- Regression coverage now pins MIME-style normalization behavior for extension-missing candidates in `SoulseekSearchHelperTests` and `MatchScorerTests`.
- Validation baseline refreshed: strict+scorer focused gate PASS (55/55) and strict+Library+scorer compatibility gate PASS (136/136).

## Strict Policy Normalization Contract Note
- `AutoDownloadStrictFilterPolicy` now applies MIME-style format normalization (including metadata suffix trimming and `x-` prefix handling) when resolving per-track/config preferred format lists.
- Adapter-boundary regression coverage now pins normalized format propagation via `AutoSearchServiceTests` so strict allowed-format constraints sent to adapter search execution are allowlist-compatible.
- Validation baseline refreshed: strict+scorer focused gate PASS (56/56) and strict+Library+scorer compatibility gate PASS (137/137).

## MIME Alias Canonicalization Contract Note
- `AutoDownloadStrictFilterPolicy`, `SoulseekSearchHelper`, and `MatchScorer` now canonicalize common MIME subtype aliases to extension-compatible tokens (`mpeg`=>`mp3`, `wave`/`vnd.wave`=>`wav`, `mp4`=>`m4a`) to preserve strict/scorer parity when extension fallback is unavailable.
- Strict preferred-format resolution now filters malformed metadata-fragment tokens (for example `codecs=...`) so comma-split MIME parameters cannot pollute adapter-bound format constraints.
- Regression coverage now pins alias canonicalization and fragment filtering in `SoulseekSearchHelperTests`, `MatchScorerTests`, and `AutoSearchServiceTests`.
- Validation baseline refreshed: strict+scorer focused gate PASS (60/60) and strict+Library+scorer compatibility gate PASS (141/141).

## Prefetch Verification Policy Parity Contract Note
- `PrefetchVerifier` format validation now resolves allowed formats via `AutoDownloadStrictFilterPolicy` instead of raw config lists, preserving per-track strict-format overrides at verification time.
- Prefetch format validation now canonicalizes MIME aliases and supports extensionless staged paths via candidate-format fallback before allowlist checks.
- Regression coverage now pins prefetch parity behavior in `PrefetchVerifierTests`.
- Validation baseline refreshed with focused prefetch contracts: PASS (2/2).

## Strict Diagnostic Format Rendering Contract Note
- `SoulseekSearchHelper.DescribeCandidate` now renders normalized format labels using extension-first normalization, candidate-format fallback, and canonical alias mapping so diagnostics remain parity-safe for extensionless MIME metadata cases.
- Diagnostic output now uses explicit `unknown` fallback when neither extension nor format metadata is usable.
- Regression coverage now pins MIME alias description labels and unknown-format fallback behavior in `SoulseekSearchHelperTests`.
- Validation baseline refreshed with helper-focused contracts: PASS (21/21), strict+scorer PASS (62/62), strict+Library+scorer PASS (143/143).

## MatchScorer Allowlist Hygiene Contract Note
- `MatchScorer` allowlist normalization now filters malformed extension tokens and falls back to default lossless allowlist semantics when caller-provided tokens normalize to empty.
- Regression coverage now pins malformed allowlist fallback behavior in `MatchScorerTests`.
- Validation baseline refreshed with scorer-focused contracts: PASS (17/17), strict+scorer PASS (63/63), strict+Library+scorer PASS (144/144).

## Adapter-Bound Format Normalization Contract Note
- `SoulseekSearchHelper.SearchCandidatesAsync` now normalizes, canonicalizes, and hygiene-filters adapter-bound `allowedFormats` before streaming calls, preserving strict/scorer normalization parity at adapter boundaries.
- Filter-token format handling now reuses strict-style normalization and malformed-token filtering before `ext:` emission.
- Regression coverage now pins adapter-bound format canonicalization behavior in `SoulseekSearchHelperTests`.
- Validation baseline refreshed: targeted adapter-bound contract PASS (1/1), helper-focused PASS (22/22), strict+scorer PASS (64/64), strict+Library+scorer PASS (145/145).

## Trusted Source Reliability Normalization Contract Note
- `MatchScorer.ScoreReliability` now compares repeated-source usernames using trimmed, case-insensitive matching to prevent reliability drift from casing or whitespace variance.
- Regression coverage now pins trusted-source normalization behavior in `MatchScorerTests`.
- Validation baseline refreshed: scorer-focused PASS (18/18), strict+scorer PASS (65/65), strict+Library+scorer PASS (146/146).

## Helper Allowlist Hygiene Parity Contract Note
- `SoulseekSearchHelper.FilterCandidates` now filters malformed configured allowlist tokens before format checks, aligning helper candidate filtering with strict/scorer allowlist hygiene semantics.
- When caller-provided allowlist tokens normalize to an empty effective set, helper candidate filtering now falls back to default lossless allowlist semantics to avoid fail-closed behavior drift.
- Regression coverage now pins malformed allowlist fallback behavior in `SoulseekSearchHelperTests`.
- Validation baseline refreshed: helper-focused PASS (23/23), strict+scorer PASS (66/66), strict+Library+scorer PASS (147/147).

## Comma-Delimited MIME Metadata Parity Contract Note
- `AutoDownloadStrictFilterPolicy`, `SoulseekSearchHelper`, and `MatchScorer` now trim format metadata at the first `;` or `,` separator so comma-delimited MIME fragments normalize consistently with semicolon-delimited values.
- Regression coverage now pins extensionless comma-delimited MIME alias canonicalization in `SoulseekSearchHelperTests` and `MatchScorerTests`.
- Validation baseline refreshed: helper-focused PASS (24/24), scorer-focused PASS (19/19), strict+scorer PASS (68/68), strict+Library+scorer PASS (149/149).

## Whitespace-Delimited MIME Metadata Parity Contract Note
- `AutoDownloadStrictFilterPolicy`, `SoulseekSearchHelper`, and `MatchScorer` now trim format metadata at the first whitespace delimiter in addition to semicolon/comma separators, preserving canonicalization parity for values like `audio/mpeg codecs=mp3`.
- Regression coverage now pins strict adapter-bound preferred-format canonicalization plus helper/scorer extensionless acceptance behavior in `AutoSearchServiceTests`, `SoulseekSearchHelperTests`, and `MatchScorerTests`.
- Validation baseline refreshed: helper-focused PASS (25/25), scorer-focused PASS (20/20), strict+scorer PASS (71/71), strict+Library+scorer PASS (152/152).

## Quoted MIME Metadata Parity Contract Note
- `AutoDownloadStrictFilterPolicy`, `SoulseekSearchHelper`, and `MatchScorer` now trim quote wrappers around MIME values before and after metadata/subtype extraction, preserving canonicalization parity for inputs like `"audio/mpeg"; charset=utf-8`.
- Regression coverage now pins quoted MIME canonicalization behavior for strict adapter-bound preferred-format inputs plus helper/scorer extensionless candidate handling in `AutoSearchServiceTests`, `SoulseekSearchHelperTests`, and `MatchScorerTests`.
- Validation baseline refreshed: helper-focused PASS (26/26), scorer-focused PASS (20/20), strict+scorer PASS (72/72), strict+Library+scorer PASS (153/153).

## Wrapper-Delimited MIME Metadata Parity Contract Note
- `AutoDownloadStrictFilterPolicy`, `SoulseekSearchHelper`, and `MatchScorer` now trim wrapper delimiters (`[]`, `()`, `{}`, `<>`, quotes) around format tokens before and after metadata/subtype extraction, preserving canonicalization parity for values like `["audio/mpeg"]; charset=utf-8`.
- Regression coverage now pins wrapper-delimited MIME behavior via strict adapter-bound preferred-format input plus helper/scorer extensionless candidate checks in `AutoSearchServiceTests`, `SoulseekSearchHelperTests`, and `MatchScorerTests`.
- Validation baseline refreshed: helper-focused PASS (26/26), scorer-focused PASS (21/21), strict+scorer PASS (73/73), strict+Library+scorer PASS (154/154).

## Risk Register Link
- [DOCS/recaps/LIBRARY_INTELLIGENCE_RISK_REGISTER.md](LIBRARY_INTELLIGENCE_RISK_REGISTER.md)
