<Window x:Class="SLSKDONET.Services.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="150" Width="350" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="ToolWindow" Background="#121212">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="PromptText" Text="Please enter a value:" Foreground="#DDD" Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="0,0,0,15" Padding="5" Background="#222" Foreground="White" BorderBrush="#444" CaretBrush="White"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" Width="70" Margin="0,0,10,0" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="0,5"/>
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" Width="70" Background="#333" Foreground="#DDD" BorderThickness="0" Padding="0,5"/>
        </StackPanel>
    </Grid>
</Window>
