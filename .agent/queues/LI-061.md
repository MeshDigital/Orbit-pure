# Slice LI-061
Status: Completed
Purpose: Pin regression contracts for MIME alias canonicalization and metadata-fragment filtering at strict/scorer/adapter boundaries.
Inputs: Tests/SLSKDONET.Tests/Services/AutoDownload/SoulseekSearchHelperTests.cs, Tests/SLSKDONET.Tests/Services/AutoDownload/MatchScorerTests.cs, Tests/SLSKDONET.Tests/Services/AutoDownload/AutoSearchServiceTests.cs.
Outputs: Regression tests that lock canonical alias behavior (`audio/mpeg` => `mp3`) and strict preferred-format fragment filtering semantics (`codecs=...` dropped).
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md and DOCS/recaps/LIBRARY_INTELLIGENCE_RECAP_PACK.md.
Validation: Included in LI-062 strict/scorer and compatibility baseline runs.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Added strict candidate filtering contract for `audio/mpeg` acceptance without extension fallback in `SoulseekSearchHelperTests`.
- Added scorer contract for MIME `audio/mpeg` alias canonicalization when filename extension is unavailable in `MatchScorerTests`.
- Added adapter-boundary contracts in `AutoSearchServiceTests` to assert `audio/mpeg` preferred formats are propagated as `mp3` and MIME parameter fragments are excluded.

Validation Results:
- Verified through LI-062 baseline gates: strict+scorer PASS (60/60), compatibility PASS (141/141).
