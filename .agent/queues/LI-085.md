# Slice LI-085
Status: Completed
Purpose: Pin adapter-bound allowed-format normalization contract in helper regression tests.
Inputs: LI-084 implementation changes.
Outputs: Targeted regression contract in `SoulseekSearchHelperTests` for adapter-bound canonical formats.
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md.
Validation: Captured in LI-086 baseline.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Added `SearchCandidatesAsync_CanonicalizesAdapterBoundAllowedFormats` in `SoulseekSearchHelperTests`.
- Contract asserts MIME alias canonicalization (`audio/mpeg` => `mp3`), dotted-extension normalization (`.FLAC` => `flac`), and malformed token suppression (`codecs=...`).

Validation Results:
- Planned in LI-086.
