# Slice LI-059
Status: Completed
Purpose: Re-open strict/scorer normalization risk intake for MIME alias canonicalization and metadata-fragment drift.
Inputs: Services/AutoDownload/AutoDownloadStrictFilterPolicy.cs, Services/AutoDownload/SoulseekSearchHelper.cs, Services/AutoDownload/MatchScorer.cs.
Outputs: Risk map identifying `audio/mpeg` alias drift (`mpeg` vs `mp3`) and comma-split metadata-fragment token leakage at strict adapter-boundary normalization.
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md and DOCS/recaps/LIBRARY_INTELLIGENCE_RISK_REGISTER.md.
Validation: Prepared for LI-060..LI-062 implementation and validation run.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Audited strict policy, strict candidate filtering, and scorer normalization parity for MIME subtype alias handling.
- Identified drift where `audio/mpeg` normalized to `mpeg` instead of allowlist-compatible `mp3` when extension fallback is unavailable.
- Identified metadata-parameter fragment leakage risk (e.g., `codecs=mp3`) from comma-split preferred-format values.

Validation Results:
- Risk intake completed and queued for implementation/contract pinning in LI-060..LI-061.
