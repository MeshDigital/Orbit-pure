# Slice LI-052
Status: Completed
Purpose: Harden strict/scorer parity for MIME-style candidate format metadata normalization.
Inputs: Services/AutoDownload/SoulseekSearchHelper.cs, Services/AutoDownload/MatchScorer.cs, strict/scorer regression suites.
Outputs: MIME-style format normalization parity across strict filtering and scorer gates, including extension-missing candidate behavior.
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md, DOCS/recaps/LIBRARY_INTELLIGENCE_RECAP_PACK.md, DOCS/recaps/LIBRARY_INTELLIGENCE_RISK_REGISTER.md.
Validation: dotnet test strict+scorer subset; dotnet test strict+Library+scorer compatibility subset.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Hardened strict filtering normalization so MIME-style format metadata (for example `audio/x-flac; charset=utf-8`) resolves to allowlist-compatible extension values before fallback checks.
- Hardened scorer normalization with the same MIME-style format parsing to preserve strict/scorer parity.
- Added regression coverage in `SoulseekSearchHelperTests` and `MatchScorerTests` for extension-missing candidates carrying MIME-style format metadata.

Validation Results:
- strict+scorer subset: PASS (55/55)
- strict+Library+scorer compatibility subset: PASS (136/136)
