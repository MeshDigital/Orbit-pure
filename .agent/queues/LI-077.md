# Slice LI-077
Status: Completed
Purpose: Re-open scorer allowlist risk intake for malformed configured extension tokens.
Inputs: Services/AutoDownload/MatchScorer.cs allowed-extension normalization path.
Outputs: Risk map identifying scorer drift when caller-provided allowlists contain malformed metadata fragments, causing unintended strict rejections.
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md and DOCS/recaps/LIBRARY_INTELLIGENCE_RISK_REGISTER.md.
Validation: Prepared for LI-078..LI-080 implementation and validation run.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Audited scorer allowed-extension normalization against strict policy token hygiene expectations.
- Identified drift where malformed allowlist tokens (for example `codecs=mp3`) could nullify valid-format matching when supplied by callers.
- Identified absence of safe fallback semantics when normalized allowlist becomes empty after hygiene filtering.

Validation Results:
- Risk intake completed and queued for implementation/contract pinning in LI-078..LI-079.
