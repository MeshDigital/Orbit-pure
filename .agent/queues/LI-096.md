# Slice LI-096
Status: Completed
Purpose: Harden helper candidate filtering allowlist hygiene with safe default fallback semantics.
Inputs: LI-095 risk map.
Outputs: `SoulseekSearchHelper.FilterCandidates` now filters malformed allowlist tokens and falls back to default lossless extensions when effective allowlist normalizes to empty.
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md.
Validation: Captured in LI-098 baseline.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Updated helper allowlist normalization in `FilterCandidates` to apply malformed-token hygiene (`IsLikelyFormatToken`) before allowlist checks.
- Added default lossless fallback (`flac`, `wav`, `aiff`, `aif`, `ape`, `alac`) when caller-supplied allowlists normalize to an empty effective set.

Validation Results:
- Planned in LI-098.
