# Slice LI-084
Status: Completed
Purpose: Harden adapter-bound format normalization in strict candidate streaming and token emission paths.
Inputs: LI-083 risk map.
Outputs: `SoulseekSearchHelper` now normalizes/canonicalizes adapter-bound allowed formats and filter-token formats with malformed-token hygiene.
Discoverability: DOCS/discoverability/LIBRARY_INTELLIGENCE_DISCOVERABILITY.md.
Validation: Captured in LI-086 baseline.
Recap: agent/recaps/LIBRARY_INTELLIGENCE_RECAP_2026-05-31.md.
Memory Update: .agent/memory/LIBRARY_INTELLIGENCE_MEMORY.md and /memories/repo/library-intelligence-slice2-progress.md.

Execution:
- Updated `SoulseekSearchHelper.SearchCandidatesAsync` to normalize, canonicalize, dedupe, and hygiene-filter `allowedFormats` before adapter-bound streaming.
- Updated `BuildFilterTokens` to reuse strict-style normalization and malformed-token filtering before ext-token emission.
- Added local `IsLikelyFormatToken` helper in `SoulseekSearchHelper` for parity with strict/scorer hygiene behavior.

Validation Results:
- Planned in LI-086.
